apiVersion: apps/v1
kind: Deployment
metadata:
  name: buildkitd
  namespace: forgejo-runner
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: buildkitd
  template:
    metadata:
      labels:
        app.kubernetes.io/name: buildkitd
    spec:
      nodeSelector:
        node-role.bakseter.net/build: 'true'
      tolerations:
        - key: node-role.bakseter.net/build
          operator: Exists
          effect: NoSchedule
      containers:
        - name: buildkitd
          image: moby/buildkit:v0.32.2@sha256:28a898719c18a33f4e8000685287fa36fd0dd9560c6440227d3a732d79bb41d8
          args:
            - --addr=tcp://0.0.0.0:1234
          ports:
            - containerPort: 1234
              name: grpc
          readinessProbe:
            exec:
              command:
                - buildctl
                - debug
                - workers
            initialDelaySeconds: 5
            periodSeconds: 30
          securityContext:
            privileged: true
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 4
              memory: 5Gi
          volumeMounts:
            - name: cache
              mountPath: /var/lib/buildkit
            - name: config
              mountPath: /etc/buildkit
      hostUsers: false
      securityContext:
        seccompProfile:
          type: Unconfined
      volumes:
        - name: cache
          emptyDir:
            sizeLimit: 5Gi
        - name: config
          configMap:
            name: buildkitd-config
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: runner
  namespace: forgejo-runner
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: runner
  template:
    metadata:
      labels:
        app.kubernetes.io/name: runner
    spec:
      containers:
        - name: runner
          image: ghcr.io/bakseter/homelab/data.forgejo.org/forgejo/runner:13@sha256:92554e3dd59ea051a4731002fb9a63497da228f63889b155141b5f844d8a48c0
          command:
            - forgejo-runner
            - daemon
            - --config
            - /config/config.yml
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 1Gi
          volumeMounts:
            - name: state
              mountPath: /state
            - name: workspace
              mountPath: /workspace
            - name: config
              mountPath: /config/config.yml
              subPath: config.yml
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      volumes:
        - name: state
          persistentVolumeClaim:
            claimName: runner-state
        - name: workspace
          emptyDir: {}
        - name: config
          configMap:
            name: runner-config
