apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cloudflared
  namespace: cloudflared
  labels:
    app: cloudflared
    release: kube-prometheus-stack
spec:
  endpoints:
  - port: metrics
  selector:
    matchLabels:
      app: cloudflared
