prometheus-blackbox-exporter:
  releaseLabel: true

  serviceMonitor:
    enabled: true
    defaults:
      interval: 60s
    labels:
      release: kube-prometheus-stack

    selfMonitor:
      enabled: true
      interval: 60s
      labels:
        release: kube-prometheus-stack
