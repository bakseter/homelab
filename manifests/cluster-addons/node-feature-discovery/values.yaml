node-feature-discovery:
  gc:
    podSecurityContext:
      seccompProfile:
        type: RuntimeDefault

  master:
    securityContext:
      seccompProfile:
        type: RuntimeDefault

  worker:
    securityContext:
      seccompProfile:
        type: RuntimeDefault
