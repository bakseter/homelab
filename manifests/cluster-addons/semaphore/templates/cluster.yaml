apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: semaphore-database
  namespace: semaphore
spec:
  instances: 1
  storage:
    size: 1Gi
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      memory: 512Mi
