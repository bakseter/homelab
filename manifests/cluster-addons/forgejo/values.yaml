forgejo:
  podSecurityContext:
    runAsUser: 65534
    runAsGroup: 65534
    fsGroup: 65534
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault

  containerSecurityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop:
        - ALL
  #   readOnlyRootFilesystem: true

  httpRoute:
    enabled: true

    parentRefs:
      - group: gateway.networking.k8s.io
        kind: Gateway
        name: tailscale-int
        namespace: tailscale

    hostnames:
      - git.int.bakseter.net

  tcpRoute:
    enabled: true

    parentRefs:
      - group: gateway.networking.k8s.io
        kind: Gateway
        name: tailscale-int
        namespace: tailscale
        sectionName: ssh

# gitea:
#   oauth:
#     - name: 'Authentik'
#       provider: 'openidConnect'
#       key: 'hello'
#       autoDiscoverUrl: 'https://authentik.bakseter.no/application/o/forgejo/.well-known/openid-configuration'
