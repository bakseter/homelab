cert-manager:
  crds:
    enabled: true

  config:
    enableGatewayAPI: true

  prometheus:
    enabled: true
    serviceMonitor:
      enabled: true
      labels:
        release: kube-prometheus-stack
