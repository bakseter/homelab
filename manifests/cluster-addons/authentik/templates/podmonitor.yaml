apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: authentik-database
  namespace: authentik
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: authentik-database
  podMetricsEndpoints:
    - port: metrics
