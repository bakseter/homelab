seerr:
  ingress:
    enabled: true
    ingressClassName: tailscale
    annotations:
      tailscale.com/tags: 'tag:k8s,tag:media'
    hosts:
      - host: seerr
        paths:
          - path: /
            pathType: Prefix
    tls:
      - hosts:
          - seerr
