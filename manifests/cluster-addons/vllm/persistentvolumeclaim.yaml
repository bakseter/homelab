apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-models-2
  namespace: vllm
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 30Gi
