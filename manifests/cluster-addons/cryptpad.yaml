apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cryptpad
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: cryptpad
    name: admin@homelab
  project: default
  source:
    path: manifests/cluster-addons/cryptpad
    repoURL: https://github.com/bakseter/homelab.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    managedNamespaceMetadata:
      labels:
        pod-security.kubernetes.io/enforce: restricted
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
