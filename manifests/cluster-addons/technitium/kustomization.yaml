apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: technitium

resources:
  - statefulset.yaml
  - httproute.yaml
  - loadbalancer.yaml
  - sealedsecret.yaml
  - service.yaml
